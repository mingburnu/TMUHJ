--drop SEQUENCE SEQ_TABLE;
--CREATE SEQUENCE SEQ_TABLE;

--drop table user;
CREATE TABLE accountNumber (
--	id BIGINT DEFAULT (NEXT VALUE FOR SEQ_TABLE) PRIMARY KEY NOT NULL,
	serNo BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	cUid VARCHAR NOT NULL,
	cDTime TIMESTAMP NOT NULL,
	uUid VARCHAR NOT NULL,
	uDTime TIMESTAMP NOT NULL,
	userID VARCHAR NOT NULL,
	userName VARCHAR NOT NULL,
	role VARCHAR NOT NULL,
	status VARCHAR NOT NULL,
);